import React, {Component} from "react";
import Table from 'react-bootstrap/Table';

class Tribal extends Component {
  constructor( props ) {
  super( props );
  }
  render() {
    // const tribeSort = this.props.eventRespone.map((data, i) => {
    //   console.log(data)
    // })
    return (
      <div>
      </div>
    );
  }
}

export default Tribal;
